define(['jquery', 'utils/GameUtils'], function($, utils) {

    //This module manages all things item-related
    var itemSystem = {
        initialize: function() {
            console.info('item system initialized!')
        }
    }

    return itemSystem;
})
